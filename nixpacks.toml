[phases.setup]
nixPkgs = ["nodejs", "pnpm"]

[phases.install]
cmds = ["corepack enable", "pnpm install"]

[phases.build]
cmds = ["pnpm --filter @workspace/hireflow build"]

[start]
cmd = "pnpm --filter @workspace/hireflow start"